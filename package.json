{
  "name": "fashion-kathmandu",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "buildfirst": "cd backend && npm install  && cd ../frontend && npm install && npm run build ",
    "build": " cd backend && npm install && tsc && cd ../frontend && npm install && npm run build ",
    "buildW": "cd backend && npm install && tsc && cd ../frontend && npm install && npm run build ",
    "start": " node backend/dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
